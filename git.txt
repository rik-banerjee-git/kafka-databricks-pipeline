git init

git config --global user.name "rik-banerjee-git"
git config --global user.email "riklearning13121999@gmail.com"

git config --global --list

git add .
git status 
git commit -m "Initial commit"
git remote -v
or
git remote add origin http://github.com/rik-banerjee-git/kafka-databricks-pipeline


git branch -M main
git push -u origin main


git pull origin main --allow-unrelated-histories